# 🚀 보험 비교 앱 B2B 대리점(GA) 구독형 배포 및 DB 설계 가이드

본 가이드는 보험 비교 앱을 여러 보험 대리점(GA) 및 소속 설계사들에게 구독료 모델로 판매하고, 대규모 마케팅 광고 트래픽을 처리하기 위한 인프라 구축 및 데이터베이스 설계 방안을 정리한 문서입니다.

---

## 1. 서버리스(Serverless) 서비스 및 호스팅 인프라 구성

별도의 물리 서버나 복잡한 백엔드 프로그램 개발 없이 **Vite(React) + Supabase** 환경을 그대로 배포하여 인프라 비용과 관리 공수를 최소화합니다.

### 🌐 웹 호스팅 (프론트엔드 화면 배포)
* **추천 서비스:** **Vercel (버셀)**
* **역할:** 사용자가 접속하는 화면(HTML/JS/CSS)을 전 세계 분산 CDN망에 업로드하여 응답 속도를 극대화합니다.
* **장점:** 
  * 광고 유입으로 동시접속자가 급증해도 다운되지 않는 글로벌 트래픽 분산 시스템.
  * 소스코드(Git)와 연동하여 업데이트 시 1분 만에 자동 빌드 및 실시간 배포.
* **예상 비용:** 초기 무료 (상용화 시 프로 등급 전환: 월 $20 / 약 2.7만 원).

### 🗄️ 백엔드 & 데이터베이스 (BaaS)
* **추천 서비스:** **Supabase (슈퍼베이스)**
* **역할:** 고객 정보, 설계사 계정 정보, 대리점(GA) 메타데이터를 저장하고 쿼리합니다.
* **장점:**
  * 백엔드 API 서버를 따로 코딩할 필요 없이 프론트엔드에서 보안 정책(RLS)을 적용하여 직접 DB에 입출력.
  * 고객이 DB를 남기는 순간 설계사 카카오톡/슬랙/구글시트 등으로 실시간 연동 자동화 가능.
* **예상 비용:** 초기 무료 (데이터 누적 시 프로 등급 전환: 월 $25 / 약 3.4만 원).

---

## 2. 대리점(GA) 및 설계사 관리 DB 스키마 설계

모든 대리점과 설계사, 고객 DB는 각각의 테이블을 따로 만들지 않고 **단 3개의 테이블을 생성한 뒤 관계형 키(Foreign Key)로 연결**하여 관리하는 것이 표준입니다.

### 🏢 1) `agencies` (대리점 정보 테이블)
구독 서비스를 구매한 대리점(GA)의 설정 정보입니다.
```sql
CREATE TABLE agencies (
  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  name text NOT NULL,                    -- 대리점명 (예: '서울종합금융')
  subscription_status text DEFAULT 'active', -- 구독 상태 (active, expired)
  lead_routing_type text DEFAULT 'direct', -- DB 분배 정책 ('direct': 개인 직송, 'distribute': 본사 분배)
  created_at timestamp DEFAULT now()
);
```

### 💼 2) `planners` (설계사 정보 테이블)
대리점에 소속되어 광고 및 상담을 진행하는 설계사 정보입니다.
```sql
CREATE TABLE planners (
  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  agency_id uuid REFERENCES agencies(id), -- 소속 대리점 ID
  planner_code text UNIQUE NOT NULL,      -- 설계사 고유넘버 (예: 'A1001', 'A1002')
  name text NOT NULL,                     -- 이름
  phone text NOT NULL,                    -- 연락처
  created_at timestamp DEFAULT now()
);
```

### 📄 3) `customer_leads` (상담 신청 고객 DB 테이블)
광고를 통해 들어온 모든 고객들의 상담 신청 기록입니다.
```sql
CREATE TABLE customer_leads (
  id bigint GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  agency_id uuid REFERENCES agencies(id),  -- 유입된 대리점 ID
  planner_id uuid REFERENCES planners(id), -- 유입된 담당 설계사 ID (개인 광고 시)
  name text NOT NULL,                      -- 고객 이름
  phone text NOT NULL,                     -- 고객 연락처
  monthly_premium numeric,                 -- 고객 입력 보험료
  raw_payload jsonb,                       -- 고객 입력 연금/건강 세부 설정 원본 데이터 백업
  status text DEFAULT 'pending',           -- 상담 처리 현황 (pending, calling, completed)
  created_at timestamp DEFAULT now()
);
```

---

## 3. 광고 집행에 따른 하이브리드 DB 수집 & 분배 비즈니스 로직

대리점의 광고 주체(회사 광고 vs 설계사 개별 광고)에 맞추어 유연하게 DB가 수집되고 열람 권한이 제어되는 알고리즘입니다.

### 🔗 개인 광고 집행 시 (개인 독립형 모드)
* **URL 형태:** `http://보험앱도메인.com/annuity?planner=A1001`
* **작동 원리:**
  1. 고객이 링크에 접속하면 프론트엔드가 주소창의 파라미터(`planner=A1001`)를 읽어옵니다.
  2. 고객이 상담 신청 버튼을 클릭하면 `customer_leads`에 `planner_id`를 `A1001` 소유자로 저장합니다.
  3. 관리자 대시보드 로그인 시 김철수 설계사는 **본인 ID로 필터링된 DB만 조회**할 수 있으며, 이영희 설계사의 고객 데이터는 열람할 수 없습니다.

### 🏢 대리점 본사 광고 집행 시 (대리점 집중 분배형 모드)
* **URL 형태:** `http://보험앱도메인.com/annuity?agency=GA_Seoul` (설계사 파라미터 없음)
* **작동 원리:**
  1. 본사 마케팅 예산으로 집행된 광고이므로 `planner_id` 없이 `agency_id`만 기록되어 DB가 수집됩니다.
  2. 수집된 DB는 대리점 관리자(점주)의 **'공용 대기 풀(Pool)'**로 들어갑니다.
  3. 대리점 관리자는 상담원 배치 상황을 고려하여 클릭 한 번으로 특정 설계사에게 담당 권한을 직접 지정해 줄 수 있습니다.

---

## 4. Supabase Row Level Security(RLS)를 통한 개인정보 보안 설정

구독형 모델에서는 여러 대리점의 설계사들이 하나의 데이터베이스를 공유하므로, **개인정보가 다른 설계사나 다른 대리점에 절대 유출되지 않도록 하는 보안(RLS)이 필수**입니다.

### 🔒 보안 조회 정책 예시 (SQL)
```sql
-- customer_leads 테이블에 RLS 적용
ALTER TABLE customer_leads ENABLE ROW LEVEL SECURITY;

-- 1. 개별 설계사는 오직 본인에게 할당되거나 본인이 모집한 고객 데이터만 조회 가능하도록 허용
CREATE POLICY "Planners can only read their own leads" 
ON customer_leads 
FOR SELECT 
USING (
  planner_id = (SELECT id FROM planners WHERE planner_code = auth.jwt() ->> 'planner_code')
);

-- 2. 대리점 관리자(점주)는 소속 대리점(agency_id)의 모든 소속 고객 데이터만 조회 가능하도록 허용
CREATE POLICY "Agency admins can read all leads in their agency" 
ON customer_leads 
FOR SELECT 
USING (
  agency_id = (SELECT agency_id FROM planners WHERE planner_code = auth.jwt() ->> 'planner_code' AND is_admin = true)
);
```

이 구조를 바탕으로 앱 서비스 배포 및 상용화 사업 설계를 진행하시면 안전하고 신속하게 시장에 안착하실 수 있습니다.
