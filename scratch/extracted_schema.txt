[ignoring loop detection]

Supabase SQL Editor에 복사하여 붙여넣고 한 번에 실행(Run)할 수 있는 **최종 전체 SQL 스크립트**입니다. 

기존의 대리점 및 설계사 구조에 **고객의 상세 나이, 보험 종류, 그리고 맞춤형 상세 보장 진단 결과(JSONB) 필드**까지 완벽히 통합되어 있습니다.

---

### 🚀 B2B 대리점 구독형 및 상세 보장 분석 DB 구축 SQL

```sql
-- 1. 대리점(GA) 정보 테이블 생성
CREATE TABLE agencies (
  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  name text NOT NULL,                                  -- 대리점명 (예: '서울종합금융')
  subscription_status text NOT NULL DEFAULT 'active',  -- 구독 상태 (active, expired)
  lead_routing_type text NOT NULL DEFAULT 'direct',    -- DB 분배 정책 ('direct': 개인 직송, 'distribute': 본사 분배)
  logo_url text,                                       -- 대리점 전용 로고 이미지 URL
<truncated 3775 bytes>