# 구현 계획서: 대시보드 도움말 가이드 모드 토글 기능

이 계획서는 신규 설계사 및 대리점 관리자가 대시보드에 처음 접속했을 때 주요 기능들을 웅장하고 직관적으로 파악할 수 있도록 돕는 "도움말 가이드 모드"를 대시보드에 직접 입히기 위한 설계서입니다.

## 사용자 검토 요구사항

> [!NOTE]
> - 도움말 가이드는 첫 로그인 시 자동으로 **ON** 상태로 시작하여 신규 설계사의 온보딩을 돕습니다.
> - 가이드를 확인한 후 꺼짐(OFF)으로 설정하면 `localStorage`에 저장(`hide_help_guide: 'true'`)되어 다음 접속부터는 불필요한 가이드 없이 깨끗한 화면만 보입니다.
> - 주요 4가지 핵심 영역에 주황색 점선 테두리(`border-dashed border-orange-500/80 animate-pulse`)와 고급 툴팁 카드를 입힙니다.

## 변경 사항 및 상세 설계

### 대시보드 컴포넌트 수정

#### [MODIFY] [AdminDashboard.tsx](file:///c:/Users/zkfnt/Desktop/insurance-comparison-main/insurance-comparison-main/src/components/AdminDashboard.tsx)
- `showHelpGuide` 상태를 추가하고 `localStorage` 값을 기준으로 초기화합니다.
- 대시보드 메인 헤더의 `{currentUser.name} 관리자 콘솔` 타이틀 바로 옆에 💡 도움말 가이드 토글 스위치(ON/OFF) 버튼을 추가합니다.
- 다음 4가지 핵심 영역에 도움말 가이드가 활성화되었을 때 주황색 점선 테두리와 SaaS 스타일의 툴팁 카드를 추가합니다:

  - **[가이드 1] 나의 랜딩페이지 링크 복사 영역** (`activeTab === 'profile'`)
    - 가이드 문구: `"🔗 이 링크를 복사하여 고객에게 전달하거나 카카오톡 프로필에 등록하면, 대표님 전용 맞춤 보험 진단 페이지로 연결됩니다!"`
  - **[가이드 2] 프로필 및 랜딩 브랜딩 설정** (`activeTab === 'profile'`)
    - 가이드 문구: `"🎨 이곳에 등록한 사진, 로고, 대표 번호, 소속 지점 주소 및 진심 어린 인사말이 고객의 보장 분석 결과 하단 카드와 푸터에 0.1초 만에 즉시 동적 반영됩니다."`
  - **[가이드 3] 실시간 고객 상담 신청 현황 (리드 목록)** (`activeTab === 'leads'`)
    - 가이드 문구: `"📋 진단을 마친 고객이 상담 신청 시 실시간으로 DB가 쌓이는 곳입니다. 상세 보기 버튼을 눌러 고객의 성별, 연령, 매칭률 및 상세 설문 결과를 확인하고 상담을 진행하세요."`
  - **[가이드 4] 대리점 전용 크레딧 및 알림 설정 (대리점 관리자 전용)** (`activeTab === 'billing'`)
    - 가이드 문구: `"💳 소속 설계사들의 보장 분석 건당 차감되는 크레딧 잔액을 확인하고 충전할 수 있습니다. 크레딧 소진 경보 번호를 등록하면 한도 소진 전 문자로 즉시 알림이 발송됩니다."`

---

## 검증 계획

### 자동화/타입 검사
- `npx tsc --noEmit` 명령어로 전체 타입 안정성을 검사합니다.
- `npm run build` 명령어로 실제 빌드가 에러 없이 완료되는지 확인합니다.

### 수동 검증
- 로컬 웹 서버를 가동하여 도움말 가이드 ON/OFF 토글이 정상 작동하는지 확인합니다.
- 브라우저를 새로고침하거나 세션이 유지될 때 `localStorage` 설정값에 따라 가이드 상태가 보존되는지 확인합니다.
- 주황색 점선 애니메이션 테두리와 다크 슬레이트 툴팁 디자인이 웅장하고 세련되게 렌더링되는지 확인합니다.
